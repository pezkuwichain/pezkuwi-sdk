# Schema: Pezkuwi SDK PRDoc Schema (prdoc) v1.0.0
# See doc at https://raw.githubusercontent.com/paritytech/pezkuwi-sdk/master/prdoc/schema_user.json

title: Renames `frame` crate to `pezkuwi-sdk-frame`

doc:
  - audience: Runtime Dev
    description: |
      This PR renames `frame` crate to `pezkuwi-sdk-frame` as `frame` is not available on crates.io. 
      Please note that this crate can only be imported as `pezkuwi-sdk-frame` or `frame`.

crates:
  - name: pezkuwi-sdk-frame
    bump: major
